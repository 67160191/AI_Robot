ช่วยวิเคราะห์โครงสร้างโปรเจกต์ของผมทั้งหมดแบบละเอียด โดยให้ตรวจสอบทั้งโครงสร้างไฟล์ โค้ด การทำงานของแต่ละส่วน และการเชื่อมต่อระหว่างระบบ

โปรเจกต์นี้เป็นระบบ AI Robot Operator สำหรับสั่งงาน PLC ผ่าน Web โดยมีส่วนประกอบหลัก เช่น Frontend, Backend, AI/LLM, Vocabulary Service, MQTT, Gateway และ Modbus TCP

สิ่งที่ต้องการให้วิเคราะห์:

1. วิเคราะห์โครงสร้างโปรเจกต์ทั้งหมด

   * แต่ละโฟลเดอร์และไฟล์มีหน้าที่อะไร
   * การแบ่ง Frontend / Backend / Service / Gateway เหมาะสมหรือไม่
   * มีไฟล์หรือส่วนไหนที่ซ้ำซ้อน ไม่จำเป็น หรือควรย้ายตำแหน่งหรือไม่
   * ตรวจสอบ Dependency และการเชื่อมต่อระหว่างแต่ละส่วน

2. วิเคราะห์ Flow การทำงานทั้งหมด

   * ตั้งแต่ผู้ใช้พิมพ์คำสั่งบน Web
   * Frontend ส่ง Request ไป Backend
   * Vocabulary Service ตรวจสอบคำสั่ง
   * AI/LLM วิเคราะห์คำสั่งในกรณีที่ Vocabulary ไม่ตรง
   * แปลงเป็น JSON Command
   * การยืนยันคำสั่ง
   * ส่งคำสั่งผ่าน MQTT
   * Gateway รับคำสั่ง
   * Gateway แปลงคำสั่งเป็น Modbus TCP
   * ส่งคำสั่งไปยัง PLC
   * PLC ตอบกลับสถานะ
   * Dashboard แสดงสถานะกลับมายังผู้ใช้

3. ตรวจหา Bug และข้อผิดพลาด
   ให้ตรวจสอบอย่างละเอียดว่ามีส่วนไหนที่:

   * ทำงานผิด
   * มีโอกาสเกิด Error
   * มี Logic ผิด
   * มีการจัดการ Error ไม่ดี
   * มี Race Condition
   * มีปัญหาเรื่อง Async/Await
   * มีปัญหาเรื่อง API
   * มีปัญหาเรื่อง MQTT
   * มีปัญหาเรื่อง Modbus TCP
   * มีปัญหาเรื่องการเชื่อมต่อ PLC
   * มีปัญหาเรื่อง JSON Command
   * มีปัญหาเรื่อง AI Parser
   * มีปัญหาเรื่อง Vocabulary Matching
   * มีปัญหาเรื่อง Machine State
   * มีปัญหาเรื่อง Security
   * มีปัญหาเรื่อง Authentication / Authorization
   * มีปัญหาเรื่อง Input Validation
   * มีปัญหาเรื่อง Logging
   * มีปัญหาเรื่องการจัดการ Connection / Reconnect
   * มีปัญหาที่อาจทำให้สั่ง PLC ผิดเครื่องหรือผิดคำสั่ง

4. ตรวจสอบความปลอดภัย
   โดยเฉพาะระบบที่สามารถส่งคำสั่งไปยัง PLC จริง ให้ตรวจสอบ:

   * การยืนยันคำสั่งก่อนส่ง
   * การป้องกันคำสั่งอันตราย
   * การตรวจสอบ Device และ Action
   * การจำกัดค่าพารามิเตอร์ เช่น Speed
   * การป้องกัน AI สร้างคำสั่งที่ไม่ถูกต้อง
   * Authentication / Authorization
   * API Security
   * MQTT Security
   * การเปิด Port
   * การเข้าถึง PLC จาก Network

5. ตรวจสอบการออกแบบ Architecture
   วิเคราะห์ว่า Architecture ปัจจุบันเหมาะสมหรือไม่ และมีจุดไหนที่ควรปรับปรุง

   ถ้าพบปัญหา ให้เสนอ Architecture ที่เหมาะสมกว่า พร้อมอธิบายว่า:

   * ควรย้ายอะไร
   * ควรเพิ่มอะไร
   * ควรตัดอะไร
   * แต่ละ Service ควรรับผิดชอบอะไร
   * Data Flow ที่ถูกต้องควรเป็นอย่างไร

6. วิเคราะห์วิธีใช้งาน
   อธิบายขั้นตอนการใช้งานระบบตั้งแต่เริ่มต้น เช่น:

   * ติดตั้ง Dependency
   * ตั้งค่า Environment
   * Start Backend
   * Start Frontend
   * Start Ollama / AI
   * Start MQTT Broker
   * Start Gateway
   * เชื่อมต่อ PLC หรือ PLC Simulator
   * ทดสอบคำสั่ง
   * ตรวจสอบสถานะ
   * ตรวจสอบ Log

   รวมถึงระบุว่าถ้าระบบส่วนใดไม่ทำงาน ควรตรวจสอบจากจุดไหนก่อน

7. วิธีแก้ Bug
   สำหรับ Bug หรือปัญหาแต่ละรายการ ให้ระบุ:

   * ปัญหาคืออะไร
   * สาเหตุที่เป็นไปได้
   * ไฟล์หรือส่วนของระบบที่เกี่ยวข้อง
   * วิธีแก้ไข
   * ตัวอย่างโค้ดที่ควรแก้ หากจำเป็น
   * วิธีทดสอบหลังแก้ไข
   * ผลลัพธ์ที่ควรได้

8. จัดลำดับความสำคัญ
   แบ่งปัญหาเป็น:

   * Critical — ต้องแก้ก่อน เพราะอาจทำให้ระบบสั่ง PLC ผิดหรือระบบเสียหาย
   * High — มีผลกระทบต่อการทำงานหลัก
   * Medium — มีผลต่อความเสถียรหรือการใช้งาน
   * Low — ปัญหาด้านโครงสร้างหรือการปรับปรุงทั่วไป

9. ตรวจสอบความพร้อมของระบบ
   สรุปว่าปัจจุบันโปรเจกต์อยู่ในระดับไหน เช่น:

   * ยังไม่พร้อมใช้งาน
   * Prototype
   * สามารถ Demo ได้
   * สามารถทดสอบกับ PLC Simulator ได้
   * พร้อมทดสอบกับ PLC จริง
   * Production Ready

10. สรุปเป็นไฟล์ `fix.md`

ไฟล์ `fix.md` ต้องมีโครงสร้างประมาณนี้:

# Project Analysis & Fix Report

## 1. Project Overview

## 2. Current Architecture

## 3. Project Structure

## 4. System Data Flow

## 5. Problems Found

### Critical

### High

### Medium

### Low

## 6. Bug Analysis

สำหรับแต่ละ Bug ให้ระบุ:

* Problem
* Cause
* Affected File
* Recommended Fix
* Example Fix
* Testing Method

## 7. Architecture Improvements

## 8. Security Issues

## 9. How to Run the Project

## 10. How to Test the System

## 11. Troubleshooting Guide

## 12. Recommended Development Order

## 13. Final Project Status

## 14. Summary

ข้อสำคัญ:

* อย่าวิเคราะห์จากการคาดเดา หากมีไฟล์หรือโค้ดจริงให้ตรวจสอบจากโค้ดก่อน
* อย่าแก้โค้ดโดยไม่อธิบายว่าทำไมต้องแก้
* ถ้าพบว่าการออกแบบบางส่วนผิด ให้บอกตรง ๆ พร้อมเสนอวิธีที่เหมาะสมกว่า
* ให้ความสำคัญกับความปลอดภัย เพราะระบบสามารถส่งคำสั่งไปยัง PLC ได้
* ตรวจสอบทั้งกรณีที่ระบบทำงานปกติและกรณีที่เกิด Error
* หากข้อมูลไม่เพียงพอ ให้ระบุว่า "ไม่สามารถยืนยันได้จากข้อมูลที่มี" แทนการเดา
* สุดท้ายให้สร้างไฟล์ `fix.md` ที่รวบรวมผลการวิเคราะห์ ปัญหา วิธีแก้ และวิธีใช้งานทั้งหมด
